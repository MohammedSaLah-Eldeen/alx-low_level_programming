#include <stdio.h>

/**
 * main - runs code inside it.
 * Return: indication of successful output.
 */
int main(void)
{
int num;

for (num = 0; num <= 9; num++)
{
printf("%d", num);
}
printf("\n");
return (0);
}
