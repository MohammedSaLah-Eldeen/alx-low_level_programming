#include <stdio.h>

/**
 * main - runs code inside it.
 * Return: indication of successful output.
 */
int main(void)
{
int i;
for (i = 48; i <= 57; i++)
{
putchar(i);
putchar(',');
putchar(' ');
}
putchar('\n');
return (0);
}
