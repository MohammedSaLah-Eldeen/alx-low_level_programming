#include <stdio.h>

/**
 * main - runs code inside it.
 * Return: indication of successful output.
 */
int main(void)
{
char z;
for (z = 'z'; z >= 'a'; z--)
{
putchar(z);
}
putchar('\n');
return (0);
}
