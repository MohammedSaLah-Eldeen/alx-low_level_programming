#include "main.h"

/**
 * _islower - runs code inside it.
 * @c:int
 * Return: indication of successful output.
 */
int _islower(int c)
{
if ((c >= 97) && (c <= 122))
{
return (1);
}
else
{
return (0);
}
}
