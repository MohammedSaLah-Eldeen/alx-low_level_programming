#include <stdio.h>
#include <stdlib.h>

/**
* main - multiplies two numbers.
* @argc: argument count
* @argv: arguments
*
* Return: 0
*/
int main(int argc, char *argv[])
{
int n1, n2;
if (argc == 1)
{
printf("Error\n");
}
else
{
n1 = argv[1];
n2 = argv[2];
printf("%d\n", n1 * n2);
}

return (0);
}
